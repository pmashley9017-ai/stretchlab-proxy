services:
  - type: web
    name: stretchlab-proxy
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: TEAM_PASSWORD
        sync: false
